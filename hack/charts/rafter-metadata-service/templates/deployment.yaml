apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rafterMetadataService.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "rafterMetadataService.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "rafterMetadataService.chart" . }}
  {{- if .Values.deployment.labels }}
    {{ include "rafterMetadataService.tplValue" ( dict "value" .Values.deployment.labels "context" . ) | nindent 4 }}
  {{- end }}
  {{- if .Values.deployment.annotations }}
  annotations:
    {{ include "rafterMetadataService.tplValue" ( dict "value" .Values.deployment.annotations "context" . ) | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.deployment.replicas }}
  {{- if .Values.deployment.extraProperties }}
  {{ include "rafterMetadataService.tplValue" ( dict "value" .Values.deployment.extraProperties "context" . ) | nindent 2 }}
  {{- end }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "rafterMetadataService.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        helm.sh/chart: {{ include "rafterMetadataService.chart" . }}
      {{- if .Values.pod.labels }}
        {{ include "rafterMetadataService.tplValue" ( dict "value" .Values.pod.labels "context" . ) | nindent 8 }}
      {{- end }}
      {{- if .Values.pod.annotations }}
      annotations:
        {{ include "rafterMetadataService.tplValue" ( dict "value" .Values.pod.annotations "context" . ) | nindent 8 }}
      {{- end }}
    spec:
      {{- if .Values.pod.extraProperties }}
      {{ include "rafterMetadataService.tplValue" ( dict "value" .Values.pod.extraProperties "context" . ) | nindent 6 }}
      {{- end }}
      containers:
        - name: {{ include "rafterMetadataService.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.pod.extraContainerProperties }}
          {{ include "rafterMetadataService.tplValue" ( dict "value" .Values.pod.extraContainerProperties "context" . ) | nindent 10 }}
          {{- end }}
          env:
            - name: APP_PORT
              value: {{ .Values.service.port.internal }}
            - name: APP_HOST
              value: {{ .Values.service.host | quote }}
            {{ include "rafterMetadataService.createEnv" ( dict "name" "APP_PROCESS_TIMEOUT" "value" .Values.envs.timeout "context" . ) | nindent 12 }}
            {{ include "rafterMetadataService.createEnv" ( dict "name" "APP_MAX_WORKERS" "value" .Values.envs.workers "context" . ) | nindent 12 }}
            - name: APP_VERBOSE
              value: {{ .Values.service.verbose }}
